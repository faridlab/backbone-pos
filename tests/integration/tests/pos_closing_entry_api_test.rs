//! PosClosingEntry API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PosClosingEntry CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// POSCLOSINGENTRY TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PosClosingEntry
pub struct PosClosingEntryTestData;

impl TestDataGenerator for PosClosingEntryTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "pos_profile_id": Uuid::new_v4().to_string(),
            "opening_entry_id": Uuid::new_v4().to_string(),
            "closed_at": now,
            "cashier_party_id": Uuid::new_v4().to_string(),
            "totals_by_method": null,
            "grand_total": 0,
            "invoice_count": 1,
            "difference_total": 0,
            "status": "draft",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "pos_profile_id": Uuid::new_v4().to_string(),
            "opening_entry_id": Uuid::new_v4().to_string(),
            "closed_at": now,
            "cashier_party_id": Uuid::new_v4().to_string(),
            "totals_by_method": null,
            "grand_total": 0,
            "invoice_count": 1,
            "difference_total": 0,
            "status": "draft",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/pos_opening_entries", &super::pos_opening_entry_api_test::PosOpeningEntryTestData).await {
            deps.push(("opening_entry_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// POSCLOSINGENTRY API TEST
// ============================================================================

/// PosClosingEntry API test suite
pub struct PosClosingEntryApiTest {
    inner: GenericCrudTest<PosClosingEntryTestData>,
}

impl PosClosingEntryApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/pos_closing_entries", "PosClosingEntry");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PosClosingEntryTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PosClosingEntryApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_pos_closing_entry_crud() {
        let mut test = PosClosingEntryApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PosClosingEntry specific tests here
// <<< CUSTOM TESTS END >>>
