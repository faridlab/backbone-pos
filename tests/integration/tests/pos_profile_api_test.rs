//! PosProfile API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PosProfile CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// POSPROFILE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PosProfile
pub struct PosProfileTestData;

impl TestDataGenerator for PosProfileTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "default_customer_id": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "income_account_id": null,
            "receivable_account_id": null,
            "cash_account_id": null,
            "write_off_account_id": null,
            "tax_account_id": null,
            "tax_rate": 0,
            "tax_template_ids": null,
            "warehouse_id": null,
            "cogs_account_id": null,
            "inventory_account_id": null,
            "allow_discount": false,
            "cash_rounding_strategy": "none",
            "cash_rounding_unit": 0,
            "status": "active",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "default_customer_id": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "income_account_id": null,
            "receivable_account_id": null,
            "cash_account_id": null,
            "write_off_account_id": null,
            "tax_account_id": null,
            "tax_rate": 0,
            "tax_template_ids": null,
            "warehouse_id": null,
            "cogs_account_id": null,
            "inventory_account_id": null,
            "allow_discount": false,
            "cash_rounding_strategy": "none",
            "cash_rounding_unit": 0,
            "status": "active",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// POSPROFILE API TEST
// ============================================================================

/// PosProfile API test suite
pub struct PosProfileApiTest {
    inner: GenericCrudTest<PosProfileTestData>,
}

impl PosProfileApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/pos_profiles", "PosProfile");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PosProfileTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PosProfileApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_pos_profile_crud() {
        let mut test = PosProfileApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PosProfile specific tests here
// <<< CUSTOM TESTS END >>>
