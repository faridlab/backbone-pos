//! Domain events for PosProfile entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PosProfile;

/// Domain events for PosProfile — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PosProfile>)` variant.
pub type PosProfileEvent = CrudEvent<PosProfile>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PosProfile events.
pub type PosProfileEventPublisher = Arc<dyn CrudEventPublisher<PosProfile> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPosProfileEventPublisher = NoOpCrudEventPublisher<PosProfile>;
