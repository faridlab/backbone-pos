//! Repository trait for PosDiscount aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PosDiscount aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PosDiscount;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosDiscountPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PosDiscountPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PosDiscountPaginatedResult {
    pub data: Vec<PosDiscount>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosDiscountFilter {
    pub company_id: Option<Uuid>,
    pub name: Option<String>,
    pub description: Option<String>,
}

impl PosDiscountFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.name.is_some() || self.description.is_some()
    }
}

/// Repository trait for PosDiscount aggregate
///
/// This trait defines the persistence contract for PosDiscount entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PosDiscountRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new pos_discount entity
    async fn save(&self, entity: &PosDiscount) -> Result<PosDiscount>;

    /// Find pos_discount by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PosDiscount>>;

    /// Find all pos_discount entities
    async fn find_all(&self) -> Result<Vec<PosDiscount>>;

    /// Update pos_discount by ID
    async fn update(&self, id: &str, entity: &PosDiscount) -> Result<Option<PosDiscount>>;

    /// Delete pos_discount by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List pos_discount with pagination
    async fn list(&self, params: PosDiscountPaginationParams) -> Result<PosDiscountPaginatedResult>;

    /// List pos_discount with pagination and filters
    async fn list_with_filters(&self, params: PosDiscountPaginationParams, filters: PosDiscountFilter) -> Result<PosDiscountPaginatedResult>;

    /// Count all pos_discount entities
    async fn count(&self) -> Result<u64>;

    /// Count pos_discount entities matching filters
    async fn count_with_filters(&self, filters: PosDiscountFilter) -> Result<u64>;

    /// Check if pos_discount exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete pos_discount by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted pos_discount by ID
    async fn restore(&self, id: &str) -> Result<Option<PosDiscount>>;

    /// List soft-deleted pos_discount entities
    async fn list_deleted(&self, params: PosDiscountPaginationParams) -> Result<PosDiscountPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save pos_discount entities
    async fn bulk_save(&self, entities: &[PosDiscount]) -> Result<Vec<PosDiscount>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
