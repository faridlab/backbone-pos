//! Repository trait for PosManagerPin aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PosManagerPin aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PosManagerPin;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosManagerPinPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PosManagerPinPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PosManagerPinPaginatedResult {
    pub data: Vec<PosManagerPin>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosManagerPinFilter {
    pub company_id: Option<Uuid>,
    pub employee_party_id: Option<Uuid>,
    pub pin_hash: Option<String>,
    pub last_attempt_ip: Option<String>,
}

impl PosManagerPinFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_party_id.is_some() || self.pin_hash.is_some() || self.last_attempt_ip.is_some()
    }
}

/// Repository trait for PosManagerPin aggregate
///
/// This trait defines the persistence contract for PosManagerPin entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PosManagerPinRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new pos_manager_pin entity
    async fn save(&self, entity: &PosManagerPin) -> Result<PosManagerPin>;

    /// Find pos_manager_pin by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PosManagerPin>>;

    /// Find all pos_manager_pin entities
    async fn find_all(&self) -> Result<Vec<PosManagerPin>>;

    /// Update pos_manager_pin by ID
    async fn update(&self, id: &str, entity: &PosManagerPin) -> Result<Option<PosManagerPin>>;

    /// Delete pos_manager_pin by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List pos_manager_pin with pagination
    async fn list(&self, params: PosManagerPinPaginationParams) -> Result<PosManagerPinPaginatedResult>;

    /// List pos_manager_pin with pagination and filters
    async fn list_with_filters(&self, params: PosManagerPinPaginationParams, filters: PosManagerPinFilter) -> Result<PosManagerPinPaginatedResult>;

    /// Count all pos_manager_pin entities
    async fn count(&self) -> Result<u64>;

    /// Count pos_manager_pin entities matching filters
    async fn count_with_filters(&self, filters: PosManagerPinFilter) -> Result<u64>;

    /// Check if pos_manager_pin exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete pos_manager_pin by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted pos_manager_pin by ID
    async fn restore(&self, id: &str) -> Result<Option<PosManagerPin>>;

    /// List soft-deleted pos_manager_pin entities
    async fn list_deleted(&self, params: PosManagerPinPaginationParams) -> Result<PosManagerPinPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save pos_manager_pin entities
    async fn bulk_save(&self, entities: &[PosManagerPin]) -> Result<Vec<PosManagerPin>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
