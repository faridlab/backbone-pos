//! Repository trait for PosClosingEntry aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PosClosingEntry aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PosClosingEntry, PosClosingStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosClosingEntryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PosClosingEntryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PosClosingEntryPaginatedResult {
    pub data: Vec<PosClosingEntry>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PosClosingEntryFilter {
    pub company_id: Option<Uuid>,
    pub pos_profile_id: Option<Uuid>,
    pub opening_entry_id: Option<Uuid>,
    pub cashier_party_id: Option<Uuid>,
    pub status: Option<PosClosingStatus>,
}

impl PosClosingEntryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.pos_profile_id.is_some() || self.opening_entry_id.is_some() || self.cashier_party_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for PosClosingEntry aggregate
///
/// This trait defines the persistence contract for PosClosingEntry entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PosClosingEntryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new pos_closing_entry entity
    async fn save(&self, entity: &PosClosingEntry) -> Result<PosClosingEntry>;

    /// Find pos_closing_entry by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PosClosingEntry>>;

    /// Find all pos_closing_entry entities
    async fn find_all(&self) -> Result<Vec<PosClosingEntry>>;

    /// Update pos_closing_entry by ID
    async fn update(&self, id: &str, entity: &PosClosingEntry) -> Result<Option<PosClosingEntry>>;

    /// Delete pos_closing_entry by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List pos_closing_entry with pagination
    async fn list(&self, params: PosClosingEntryPaginationParams) -> Result<PosClosingEntryPaginatedResult>;

    /// List pos_closing_entry with pagination and filters
    async fn list_with_filters(&self, params: PosClosingEntryPaginationParams, filters: PosClosingEntryFilter) -> Result<PosClosingEntryPaginatedResult>;

    /// Count all pos_closing_entry entities
    async fn count(&self) -> Result<u64>;

    /// Count pos_closing_entry entities matching filters
    async fn count_with_filters(&self, filters: PosClosingEntryFilter) -> Result<u64>;

    /// Check if pos_closing_entry exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete pos_closing_entry by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted pos_closing_entry by ID
    async fn restore(&self, id: &str) -> Result<Option<PosClosingEntry>>;

    /// List soft-deleted pos_closing_entry entities
    async fn list_deleted(&self, params: PosClosingEntryPaginationParams) -> Result<PosClosingEntryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save pos_closing_entry entities
    async fn bulk_save(&self, entities: &[PosClosingEntry]) -> Result<Vec<PosClosingEntry>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
