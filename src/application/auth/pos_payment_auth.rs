//! Auth configuration for PosPayment handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PosPayment>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PosPaymentService;
use crate::domain::entity::PosPayment;

/// Permission string constants for PosPayment operations.
pub mod permissions {
    pub const LIST:        &str = "pos_payment:list";
    pub const READ:        &str = "pos_payment:read";
    pub const CREATE:      &str = "pos_payment:create";
    pub const UPDATE:      &str = "pos_payment:update";
    pub const DELETE:      &str = "pos_payment:delete";
    pub const RESTORE:     &str = "pos_payment:restore";
    pub const TRASH:       &str = "pos_payment:trash";
    pub const EMPTY_TRASH: &str = "pos_payment:empty_trash";
    pub const BULK_CREATE: &str = "pos_payment:bulk_create";
    pub const UPSERT:      &str = "pos_payment:upsert";
}

/// Resource policy for PosPayment — maps CRUD actions to permission strings.
pub struct PosPaymentPolicy;

#[async_trait]
impl ResourcePolicy<PosPayment> for PosPaymentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PosPayment,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PosPayment handlers.
///
/// Wraps `PosPaymentService` and checks `PosPaymentPolicy` before delegating.
pub type PosPaymentGuard = PermissionGuard<PosPayment>;

/// Service-integrated permission guard for PosPayment (for use in generated handlers).
pub type PosPaymentServiceGuard = ServicePermissionGuard<PosPayment, PosPaymentService, PosPaymentPolicy>;

// <<< CUSTOM
// END CUSTOM
