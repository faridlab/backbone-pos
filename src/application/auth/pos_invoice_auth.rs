//! Auth configuration for PosInvoice handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PosInvoice>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PosInvoiceService;
use crate::domain::entity::PosInvoice;

/// Permission string constants for PosInvoice operations.
pub mod permissions {
    pub const LIST:        &str = "pos_invoice:list";
    pub const READ:        &str = "pos_invoice:read";
    pub const CREATE:      &str = "pos_invoice:create";
    pub const UPDATE:      &str = "pos_invoice:update";
    pub const DELETE:      &str = "pos_invoice:delete";
    pub const RESTORE:     &str = "pos_invoice:restore";
    pub const TRASH:       &str = "pos_invoice:trash";
    pub const EMPTY_TRASH: &str = "pos_invoice:empty_trash";
    pub const BULK_CREATE: &str = "pos_invoice:bulk_create";
    pub const UPSERT:      &str = "pos_invoice:upsert";
}

/// Resource policy for PosInvoice — maps CRUD actions to permission strings.
pub struct PosInvoicePolicy;

#[async_trait]
impl ResourcePolicy<PosInvoice> for PosInvoicePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PosInvoice,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PosInvoice handlers.
///
/// Wraps `PosInvoiceService` and checks `PosInvoicePolicy` before delegating.
pub type PosInvoiceGuard = PermissionGuard<PosInvoice>;

/// Service-integrated permission guard for PosInvoice (for use in generated handlers).
pub type PosInvoiceServiceGuard = ServicePermissionGuard<PosInvoice, PosInvoiceService, PosInvoicePolicy>;

// <<< CUSTOM
// END CUSTOM
