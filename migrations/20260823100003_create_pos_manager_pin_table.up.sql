-- Migration: Create pos_manager_pins table
-- Generated by metaphor-schema

CREATE SCHEMA IF NOT EXISTS pos;

CREATE TABLE IF NOT EXISTS pos.pos_manager_pins (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    company_id UUID NOT NULL,
    employee_party_id UUID NOT NULL,
    pin_hash TEXT NOT NULL,
    failed_attempts INTEGER NOT NULL DEFAULT 0 CHECK (failed_attempts >= 0),
    locked_until TIMESTAMPTZ,
    last_attempt_at TIMESTAMPTZ,
    last_attempt_ip TEXT,
    metadata JSONB NOT NULL DEFAULT '{"created_at":null,"updated_at":null,"deleted_at":null,"created_by":null,"updated_by":null,"deleted_by":null}'::jsonb,
    PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_pos_manager_pins_company_id_employee_party_id ON pos.pos_manager_pins (company_id, employee_party_id) WHERE (metadata->>'deleted_at') IS NULL;

-- GIN index for audit metadata JSONB queries
CREATE INDEX IF NOT EXISTS idx_pos_manager_pins_metadata_gin ON pos.pos_manager_pins USING GIN (metadata);
CREATE INDEX IF NOT EXISTS idx_pos_manager_pins_metadata_deleted_at ON pos.pos_manager_pins ((metadata->>'deleted_at'));
CREATE INDEX IF NOT EXISTS idx_pos_manager_pins_metadata_created_at ON pos.pos_manager_pins ((metadata->>'created_at'));
CREATE INDEX IF NOT EXISTS idx_pos_manager_pins_metadata_updated_at ON pos.pos_manager_pins ((metadata->>'updated_at'));

-- Triggers for automatic metadata timestamp management
-- Automatically sets created_at on INSERT and updated_at on UPDATE

-- Function to set metadata->'created_at' on INSERT
CREATE OR REPLACE FUNCTION pos.pos_manager_pins_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS pos_manager_pins_insert_audit ON pos.pos_manager_pins;
CREATE TRIGGER pos_manager_pins_insert_audit BEFORE INSERT ON pos.pos_manager_pins
    FOR EACH ROW EXECUTE FUNCTION pos.pos_manager_pins_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS pos_manager_pins_update_audit ON pos.pos_manager_pins;
CREATE TRIGGER pos_manager_pins_update_audit BEFORE UPDATE ON pos.pos_manager_pins
    FOR EACH ROW EXECUTE FUNCTION pos.pos_manager_pins_audit_timestamp();
